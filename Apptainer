bootstrap: docker
from: pytorch/pytorch:2.2.0-cuda12.1-cudnn8-runtime

%files
requirements.txt


%post
# need g++
apt-get update
apt-get install build-essential --assume-yes
apt-get clean --assume-yes

yes | pip install --requirement /requirements.txt
